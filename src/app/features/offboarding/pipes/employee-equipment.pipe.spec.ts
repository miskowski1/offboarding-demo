import { EmployeeEquipmentPipe } from './employee-equipment.pipe';

describe('EmployeeEquipmentPipe', () => {
  it('create an instance', () => {
    const pipe = new EmployeeEquipmentPipe();
    expect(pipe).toBeTruthy();
  });
});
